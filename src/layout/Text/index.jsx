import { Container } from './styles';
import { HighlightEditable } from '../../components/HighlightEditable';

export const Text = () => {
    return (
        <Container>
            <HighlightEditable />
        </Container>
    );
};
