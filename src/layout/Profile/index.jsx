import { Container } from './styles';
import ProfileMenu from '../../components/ProfileMenu';

const Profile = () => {
    return (
        <Container>
            <ProfileMenu />
        </Container>
    );
};

export default Profile;
