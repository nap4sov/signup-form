import styled from 'styled-components';
import { BasicButton, FlexContainer } from '../../styles';

export const Button = styled(BasicButton)`
    flex-grow: 2;
`;

export const Container = styled(FlexContainer)`
    gap: 10px;
    margin-bottom: 20px;
`;
